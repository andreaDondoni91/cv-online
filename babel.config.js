// Used by Jest only — Gatsby's own build ignores a root babel.config.js and
// applies babel-preset-gatsby itself, so this file exists purely so tests can
// transform JSX/ESM the same way.
module.exports = {
  presets: ["babel-preset-gatsby"],
};
